<h1>Items#index</h1>

<% if @items.empty? %>
  <p><strong>No Items</strong></p>
<% end %>


<% @items.each do |item| %>
<div class="item">
  <h2><%= item.name %></h2><%= link_to 'Edit', edit_item_path(item) %>
</div>
<% end %>
