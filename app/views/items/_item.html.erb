<div class="item">
  <h2><%= link_to item.name, item %></h2>
  <div class="details">
    <%#= number_to_currency(product.price) %>
    <% if item.item_type %>
      Item Type: <%= item.item_type.name %>
    <% end %>
  </div>
</div>
