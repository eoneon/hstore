<%= form_for(@artist) do |f| %>
  <div class="field">
    <%= f.label :first_name %><br>
    <%= f.text_field :first_name %>
  </div>

  <div class="field">
    <%= f.label :last_name %><br>
    <%= f.text_field :last_name %>
  </div>

  <!-- <div class="field">
    <%#= f.fields_for :displays, @artist.displays.first do |f| %>
      <%#= f.text_field :name, placeholder: "display name" %>
      <%#= render 'field_fields', f: f %>
    <%# end %>
  </div> -->

  <div class="field">
    <%= f.fields_for :displays, @artist.displays.first do |f| %>
      <%#= f.text_field :name, placeholder: "display name" %>
      <%= render 'display_fields', f: f %>
    <% end %>
  </div>

  <div class="form-group">
    <%= link_to_add_fields "Add Field", f, :displays, "artists" %>
    <%= f.submit "Save", class: 'btn btn-success' %>
  </div>
<% end %>
