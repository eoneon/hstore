<fieldset>
  <%= f.label :artist_name %><br>
  <%= f.collection_select( :artist_ids, Artist.all, :id, :full_name, prompt: "- Artist -", include_blank: true) %>
  <%= f.hidden_field :_destroy %>
  <%= link_to "remove", '#', class: "remove_fields" %>
</fieldset>
