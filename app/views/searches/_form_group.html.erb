<%= form_tag new_search_path, method: :get do %>
  <%= select_tag :item_type_id, options_from_collection_for_select(ItemType.all, :id, :name), { prompt: "- Select Item Type -" } %>
  <%= submit_tag "New Search", name: nil %>
<% end %>
