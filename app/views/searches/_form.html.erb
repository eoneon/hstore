<%= form_for @search, :html => {:id => 'search_form'} do |f| %>
  <%= render "items/hidden", f: f %>

  <script type="text/javascript">
    function submitform(){
      $('#redirect_location').val(':edit');
      $('#search_form').submit();
    }
  </script>

  <%= render "items/properties", parent: @search, f: f %>
  <%= f.submit "Search" %>
    <%# if params[:action] == 'edit' %>
    <%# end %>
<% end %>
